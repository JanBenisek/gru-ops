apiVersion: kustomize.toolkit.fluxcd.io/v1beta1
kind: Kustomization
metadata:
  name: docker-registry
  namespace: flux-system
spec:
  path: ./gitops/manifests/docker-registry
  interval: 15m
  prune: true
  timeout: 10m
  validation: server
  dependsOn:
    - name: external-dns
      namespace: flux-system
    - name: openebs
      namespace: flux-system
  sourceRef:
    kind: GitRepository
    name: flux-system
  postBuild:
    substituteFrom:
    - kind: ConfigMap
      name: gru-values
  # healthChecks:
  #   - apiVersion: apps/v1
  #     kind: Deployment
  #     name: docker-registry
  #     namespace: docker-registry