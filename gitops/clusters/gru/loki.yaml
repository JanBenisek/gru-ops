apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: loki
  namespace: flux-system
spec:
  dependsOn:
    - name: external-dns
      namespace: flux-system
  interval: 15m
  path: ./gitops/manifests/monitoring/loki
  prune: true 
  timeout: 20m
  postBuild:
    substituteFrom:
    - kind: ConfigMap
      name: gru-values
  sourceRef:
    kind: GitRepository
    name: flux-system
  validation: server
  # healthChecks:
  #   - apiVersion: apps/v1
  #     kind: Deployment
  #     name: kube-prometheus-stack-grafana
  #     namespace: monitoring