apiVersion: kustomize.toolkit.fluxcd.io/v1beta1
kind: Kustomization
metadata:
  name: homepage
  namespace: flux-system
spec:
  path: ./gitops/manifests/homepage
  interval: 15m
  prune: true 
  timeout: 20m 
  validation: server
  dependsOn:
    - name: external-dns
      namespace: flux-system
  sourceRef:
    kind: GitRepository
    name: flux-system
  postBuild:
    substituteFrom:
    - kind: ConfigMap
      name: gru-values
  # healthChecks:
  #   - apiVersion: apps/v1
  #     kind: Deployment
  #     name: homepage
  #     namespace: homepage