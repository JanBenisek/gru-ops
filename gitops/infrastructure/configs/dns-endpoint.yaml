apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: batcave.example.com
  namespace: batcave
spec:
  endpoints:
  - dnsName: penguins.example.com
    recordTTL: 180
    recordType: A
    targets:
    - 192.168.178.39 # THIS LIKELY NEEDS TO BE FIXED and it is missing LoadBalancer