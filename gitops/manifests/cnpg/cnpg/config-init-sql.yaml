apiVersion: v1
kind: ConfigMap
metadata:
  name: config-init-sql
  namespace: cnpg

data:
  init.sql: |
    CREATE SCHEMA IF NOT EXISTS my_schema AUTHORIZATION myuser;
    GRANT ALL PRIVILEGES ON SCHEMA my_schema TO myuser;