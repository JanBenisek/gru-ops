apiVersion: k8up.io/v1
kind: Backup
metadata:
  name: backup-hetzner
  namespace: k8up
spec:
  backend:
    s3:
      endpoint: https://hel1.your-objectstorage.com
      bucket: pengiunsbackup
      accessKeyIDSecretRef:
        name: hetzner-credentials
        key: username
      secretAccessKeySecretRef:
        name: hetzner-credentials
        key: password
    repoPasswordSecretRef:
      name: restic-credentials
      key: password