apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: monitoring
  namespace: monitoring
spec:
  releaseName: monitoring
  chart:
    spec:
      chart: monitoring
      version: "8.5.10"
      sourceRef:
        kind: HelmRepository
        name: monitoring
        namespace: monitoring
  interval: 50m
  # values:
    