kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: pvc-hiker
  namespace: hiker
spec:
  storageClassName: local-hostpath-kevin
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1G
