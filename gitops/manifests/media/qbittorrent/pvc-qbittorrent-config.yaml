kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: pvc-qbittorrent-config
  namespace: media
spec:
  storageClassName: local-hostpath-stuart-data
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi
