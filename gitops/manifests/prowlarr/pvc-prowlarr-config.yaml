kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: pvc-prowlarr-config
  namespace: prowlarr
spec:
  storageClassName: local-hostpath-stuart-data
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi
