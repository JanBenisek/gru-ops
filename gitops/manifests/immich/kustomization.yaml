apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: immich
resources:
  - namespace.yaml
  - pvc-immich.yaml
  - release.yaml
  - repository.yaml
  - release-proxy.yaml
  - repository-proxy.yaml
  # Backups
  - backup-immich-manual.yaml
  - backup-immich-schedule.yaml
