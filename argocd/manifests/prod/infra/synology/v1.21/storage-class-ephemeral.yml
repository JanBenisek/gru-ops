# LUN storage for JupyterHub user volumes - auto-deleted when PVC is removed
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  annotations:
    storageclass.kubernetes.io/is-default-class: "false"
    argocd.argoproj.io/sync-wave: "40"
  name: synology-storage-jupyter
provisioner: csi.san.synology.com
parameters:
  fsType: 'ext4'
  dsm: '192.168.178.96'
  location: '/volume1'
  thinProvision: "true"        # lets me allocate more logical storage than the physical storage actually available
  spaceReclamation: "true"     # enable automatic space reclamation
reclaimPolicy: Delete
allowVolumeExpansion: true
