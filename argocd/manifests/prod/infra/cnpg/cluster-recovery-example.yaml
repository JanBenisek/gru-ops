# # Recovery cluster example - USE THIS ONLY FOR DISASTER RECOVERY
# # This creates a NEW cluster from backup, does NOT modify existing cluster
# #
# # Usage:
# # 1. Update metadata.name to a new cluster name (e.g., cnpg-restored)
# # 2. Optionally set recoveryTarget for point-in-time recovery
# # 3. kubectl apply -f cluster-recovery-example.yaml
# # 4. Wait for recovery to complete: kubectl get cluster -n cnpg -w
# # 5. Update your apps to point to new cluster if needed

# apiVersion: postgresql.cnpg.io/v1
# kind: Cluster
# metadata:
#   name: cnpg-restored  # CHANGE THIS - must be different from original
#   namespace: cnpg
# spec:
#   instances: 1

#   superuserSecret:
#     name: superuser-pswd

#   enableSuperuserAccess: true

#   resources:
#     requests:
#       memory: "2Gi"
#       cpu: "200m"
#     limits:
#       memory: "6Gi"

#   imageName: "ghcr.io/tensorchord/cloudnative-vectorchord:18.0-0.5.3"
#   imagePullPolicy: Always

#   postgresql:
#     shared_preload_libraries:
#       - "vchord"

#   storage:
#     size: 20Gi
#     storageClass: synology-storage-apps

#   # Bootstrap from backup
#   bootstrap:
#     recovery:
#       source: cnpg-backup

#       # OPTIONAL: Point-in-Time Recovery
#       # Uncomment ONE of these to restore to a specific point:

#       # recoveryTarget:
#       #   # Option 1: Restore to specific timestamp
#       #   targetTime: "2026-03-24 14:30:00.00000+00"

#       #   # Option 2: Restore to specific transaction ID
#       #   targetXID: "123456"

#       #   # Option 3: Restore to named restore point
#       #   targetName: "before_migration"

#       #   # Option 4: Restore immediately (latest consistent state)
#       #   targetImmediate: true

#   # Reference to backup location
#   externalClusters:
#     - name: cnpg-backup
#       barmanObjectStore:
#         destinationPath: s3://gru-k8up-backups/barman/postgres/cnpg-cluster/
#         endpointURL: https://fsn1.your-objectstorage.com
#         s3Credentials:
#           accessKeyId:
#             name: hetzner-credentials
#             key: user
#           secretAccessKey:
#             name: hetzner-credentials
#             key: password
#         wal:
#           compression: gzip
#           encryption: AES256

#   # Optional: Continue backing up the restored cluster
#   backup:
#     barmanObjectStore:
#       destinationPath: s3://gru-k8up-backups/postgres/cnpg-restored/
#       endpointURL: https://fsn1.your-objectstorage.com
#       s3Credentials:
#         accessKeyId:
#           name: hetzner-credentials
#           key: user
#         secretAccessKey:
#           name: hetzner-credentials
#           key: password
#       data:
#         compression: gzip
#         encryption: AES256
#       wal:
#         compression: gzip
#         encryption: AES256
#     retentionPolicy: "30d"

#   managed:
#     services:
#       disabledDefaultServices: ["ro", "r"]
#       additional:
#         - selectorType: rw
#           serviceTemplate:
#             metadata:
#               name: cnpg-restored-rw-lb
#               annotations:
#                 external-dns.alpha.kubernetes.io/hostname: vchord-postgres-restored.pengiuns.com
#             spec:
#               type: LoadBalancer
#               loadBalancerIP: 192.168.178.191  # CHANGE THIS - use different IP
#               ports:
#                 - name: postgresql
#                   port: 5432
#                   targetPort: 5432
#                   protocol: TCP
#           updateStrategy: patch
