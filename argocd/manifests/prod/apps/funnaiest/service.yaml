apiVersion: v1
kind: Service
metadata:
  name: funnaiest
  namespace: funnaiest
  labels:
    app.kubernetes.io/name: funnaiest
spec:
  selector:
    app: funnaiest
  ports:
  - name: http
    port: 80 # we talk to 80
    protocol: TCP
    targetPort: 9090 # app listens here