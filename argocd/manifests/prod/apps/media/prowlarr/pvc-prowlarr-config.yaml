kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: pvc-prowlarr-config
  namespace: media
spec:
  storageClassName: synology-storage-configs
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi
