apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-hiker
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteMany
  nfs:
    server: 192.168.178.96
    path: /volume2/pvc-hiker   # make sure this subdirectory exists or is writable
  persistentVolumeReclaimPolicy: Delete
